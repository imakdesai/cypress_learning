{
    "baseUrl": "${{ github.BASE_URL }}",
    "ignore_uncaught": true,
    "env": {
        "environment": "credci",
        "userName" : "${{ github.PASSWORD }}",
        "password" : "${{ github.USERNAME }}"
    }
  }

  